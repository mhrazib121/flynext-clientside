import React from 'react';
import { Row } from 'react-bootstrap';

const MyBooking = () => {
    return (
        <div>

            <Row xs={1} md={2} className="g-4">
                {/* {
                    services.map(service => <BookingItem key={service.id} Booking={Booking} />)
                } */}
            </Row>
        </div>
    );
};

export default MyBooking;